---
version: "3"

vars:
  CLUSTER_DIR: "{{.ROOT_DIR}}/clusters/lovenet"

env:
  KUBECONFIG: "{{.CLUSTER_DIR}}/kubeconfig"

includes:
  fx: .taskfiles/Flux/Tasks.yml

tasks:

  default:
    silent: true
    cmds: ["task -l"]
