# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
    name: vikunja
    namespace: collab
type: Opaque
stringData:
    VIKUNJA_SERVICE_JWT: "${SECRET_VIKUNJA_SERVICE_JWT}"
    VIKUNJA_DATABASE_USER: "${SECRET_VIKUNJA_DATABASE_USER}"
    VIKUNJA_DATABASE_PASSWORD: "${SECRET_VIKUNJA_DATABASE_PASSWORD}"
