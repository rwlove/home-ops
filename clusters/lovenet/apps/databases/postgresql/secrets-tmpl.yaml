apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
    name: postgres-superuser
    namespace: databases
stringData:
    username: "${SECRET_POSTGRES_SUPERUSER_USERNAME}"
    password: "${SECRET_POSTGRES_SUPERUSER_PASSWORD}"
