# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: autobrr
  namespace: downloads
type: Opaque
stringData:
  POSTGRES_USER: "${SECRET_AUTOBRR_POSTGRES_USER}"
  POSTGRES_PASS: "${SECRET_AUTOBRR_POSTGRES_PASS}"
  SESSION_SECRET: "${SECRET_AUTOBRR_SESSION_SECRET}"
