# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
    name: appdaemon-helm-values
    namespace: home
type: Opaque
stringData:
    deployKeyBase64: "${SECRET_HASS_DEPLOY_KEY_B64}"
    HA_URL: "${SECRET_HASS_SERVER}"
    TOKEN: "${SECRET_HASS_TOKEN}"
