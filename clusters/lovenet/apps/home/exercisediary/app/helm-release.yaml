---
# yaml-language-server: $schema=https://kubernetes-schemas.thesteamedcrab.com/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app exercisediary
  namespace: home
spec:
  interval: 30m
  chart:
    spec:
      # renovate: registryUrl=https://bjw-s.github.io/helm-charts
      chart: app-template
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system

  maxHistory: 3

  install:
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  uninstall:
    keepHistory: false

  values:
    controllers:
      main:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          main:
            image:
              repository: aceberg/exercisediary
              tag: 0.1.2@sha256:f10a177683f431517887591ff261e6362d06dc665afd54530c511582a734e366

            env:
              TZ: ${TIMEZONE}         # required, default: ""
              HOST: "0.0.0.0"         # optional, default: 0.0.0.0
              PORT: "8851"            # optional, default: 8851
              THEME: "darkly"         # optional, default: grass
              COLOR: "dark"           # optional, default: light

            resources:
              requests:
                cpu: 15m
                memory: 64M
              limits:
                memory: 64M

    service:
      main:
        ports:
          http:
            port: 8851

    ingress:
      main:
        enabled: true
        className: nginx
        annotations:
          hajimari.io/enable: "true"
          hajimari.io/appName: "Exercise Diary"
          hajimari.io/icon: mdi:radio
          hajimari.io/group: Home
          hajimari.io/instance: "user,admin"
        hosts:
          - host: &host "{{ .Release.Name }}.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  name: main
                  port: http
        tls:
          - hosts:
              - *host

    persistence:
      config:
        enabled: true
        existingClaim: exercisediary-data-pvc
        globalMounts:
          - path: /data/ExerciseDiary
