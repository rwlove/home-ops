---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: frigate-media-pv
  labels:
    type: nfs
spec:
  storageClassName: frigate-media-storage-class
  capacity:
    storage: 300Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: "${SECRET_NFS_HOST_FRIGATE}"
    path: /mnt/frigate-media
