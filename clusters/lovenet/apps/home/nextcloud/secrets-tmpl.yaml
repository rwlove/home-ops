# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: nextcloud
  namespace: home
type: Opaque
stringData:
  SECRET_NEXTCLOUD_ADMIN_PASSWORD: "${NEXTCLOUD_ADMIN_PASSWORD}"
  SECRET_NEXTCLOUD_MAIL_USER: "${NEXTCLOUD_MAIL_USER}"
  SECRET_NEXTCLOUD_MAIL_PASSWORD: "${NEXTCLOUD_MAIL_PASSWORD}"
  SECRET_NEXTCLOUD_POSTGRES_USER: "${NEXTCLOUD_POSTGRES_USER}"
  SECRET_NEXTCLOUD_POSTGRES_PASS: "${NEXTCLOUD_POSTGRES_PASS}"
  POSTGRES_USER: "${NEXTCLOUD_POSTGRES_USER}"
  POSTGRES_PASS: "${NEXTCLOUD_POSTGRES_PASS}"
