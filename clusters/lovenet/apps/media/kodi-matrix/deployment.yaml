---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kodi-matrix
  namespace: media
spec:
  selector:
    matchLabels:
      app: kodi-matrix
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: kodi-matrix
    spec:
      containers:
        - name: kodi-matrix
          image: celedhrim/kodi-server:matrix
          env:
            - name: TZ
              value: "America/New_York"
              # - name: PUID
              #   value: '0'
              # - name: PGID
              #   value: '0'
          ports:
            - name: http
              containerPort: 8089
              protocol: TCP
            - name: ws
              containerPort: 9090
              protocol: TCP
          volumeMounts:
            - name: kodi-matrix-config
              mountPath: /config/.kodi
      volumes:
        - name: kodi-matrix-config
          nfs:
            server: brain.thesteamedcrab.com
            path: /mnt/kubernetes/kodi-matrix
