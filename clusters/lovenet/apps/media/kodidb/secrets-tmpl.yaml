# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: kodidb-helm-values
  namespace: media
data:
  SECRET_MARIADB_ROOT_PASSWORD: "${KODIDB_MYSQL_ROOT_PASSWORD}"
  SECRET_MARIADB_USER: "${KODIDB_MYSQL_USER}"
  SECRET_MARIADB_PASSWORD: "${KODIDB_MYSQL_PASSWORD}"
