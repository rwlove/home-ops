# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
    name: stash-helm-values
    namespace: media
type: Opaque
stringData:
    SECRET_STASH_NFS_HOST: "${STASH_NFS_HOST}"
    SECRET_STASH_NFS_PATH: "${STASH_NFS_PATH}"
