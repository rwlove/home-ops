# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
stringData:
  admin-user: ${SECRET_GRAFANA_ADMIN_USER}
  admin-password: ${SECRET_GRAFANA_ADMIN_PASSWORD}

---
apiVersion: v1
kind: Secret
metadata:
  name: grafana
  namespace: monitoring
stringData:
  GF_DATABASE_USER: ${SECRET_GRAFANA_DB_USER}
  GF_DATABASE_PASSWORD: ${SECRET_GRAFANA_DB_PASSWORD}
