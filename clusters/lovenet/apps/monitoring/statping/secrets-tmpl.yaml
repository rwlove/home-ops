# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
    name: statping
    namespace: monitoring
type: Opaque
stringData:
    POSTGRES_USER: "${SECRET_STATPING_POSTGRES_USER}"
    POSTGRES_PASS: "${SECRET_STATPING_POSTGRES_PASS}"
