---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: dashboard
  namespace: network
spec:
  endpoints:
  - dnsName: "traefik.${SECRET_DOMAIN}"
    recordType: A
    targets:
    - traefik.thesteamedcrab.com
    # - "${SVC_TRAEFIK_ADDRESS}"
