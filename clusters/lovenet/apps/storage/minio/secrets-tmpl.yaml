# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
    name: minio
    namespace: storage
stringData:
    MINIO_ROOT_USER: "${SECRET_MINIO_ROOT_USER}"
    MINIO_ROOT_PASSWORD: "${SECRET_MINIO_ROOT_PASSWORD}"
