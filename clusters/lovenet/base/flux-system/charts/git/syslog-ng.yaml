---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: syslog-ng
  namespace: flux-system
spec:
  interval: 10m
  url: https://github.com/syslog-ng/syslog-ng
  ref:
    branch: main
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/contrib/syslog-ng-helm-chart/
