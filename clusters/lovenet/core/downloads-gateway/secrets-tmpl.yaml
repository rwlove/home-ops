# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: downloads-gateway-vpnconfig
  namespace: downloads
stringData:
  VPN_SERVICE_PROVIDER: "${SECRET_DLGW_VPN_SERVICE_PROVIDER}"
  WIREGUARD_PRIVATE_KEY: "${SECRET_DLGW_WIREGUARD_PRIVATE_KEY}"
  WIREGUARD_ADDRESSES: "${SECRET_DLGW_WIREGUARD_ADDRESSES}"
  SERVER_CITIES: "${SECRET_DLGW_SERVER_CITIES}"
