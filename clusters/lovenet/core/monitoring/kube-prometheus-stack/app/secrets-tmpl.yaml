# yamllint disable
---
apiVersion: v1
kind: Secret
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
type: Opaque
stringData:
  SECRET_ALERTMANAGER_PUSHOVER_USER_KEY: "${ALERTMANAGER_PUSHOVER_USER_KEY}"
  SECRET_ALERTMANAGER_PUSHOVER_TOKEN: "${ALERTMANAGER_PUSHOVER_TOKEN}"
