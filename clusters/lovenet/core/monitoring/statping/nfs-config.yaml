---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: statping-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: longhorn-static
  volumeName: statping-data
