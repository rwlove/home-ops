---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: syslog-ng-pv
  labels:
    type: nfs
spec:
  storageClassName: syslog-storage-class
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: "${SECRET_NFS_HOST_0}"
    path: /mnt/kubernetes/syslog
