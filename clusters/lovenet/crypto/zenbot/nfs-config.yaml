---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: zenbot-config
  labels:
    type: nfs
spec:
  storageClassName: zenbot-config-storage-class
  capacity:
    storage: 300Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: brain.thesteamedcrab.com
    path: /mnt/mass_storage/kubernetes/zenbot/config
