---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hivemq
  namespace: home
spec:
  interval: 5m
  chart:
    spec:
      chart: hivemq-operator
      version: 0.8.3
      sourceRef:
        kind: HelmRepository
        name: hivemq-charts
        namespace: flux-system
      interval: 10m
#  values:
