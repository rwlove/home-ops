---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pihole-config-pv
  labels:
    type: nfs
spec:
  storageClassName: pihole-config-storage-class
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: brain.thesteamedcrab.com
    path: /mnt/kubernetes/pihole
