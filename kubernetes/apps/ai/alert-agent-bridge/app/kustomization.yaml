---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cnp-allow.yaml
  - ./externalsecret.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: alert-agent-bridge
    files:
      - bridge.py=./resources/bridge.py
generatorOptions:
  disableNameSuffixHash: true
