---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
resources:
  - ./namespace.yaml
  - ./priority-class.yaml
  - ./comfyui/ks.yaml
  - ./kubeclaw/ks.yaml
  - ./langgraph-agents/ks.yaml
  - ./mcp-inspector/ks.yaml
  - ./ollama/ks.yaml
  - ./paperless-ai/ks.yaml
  - ./sync-receiver/ks.yaml
  # khoj scaffolded but disabled — uncomment after:
  #   1. Create 1Password item `khoj` with: POSTGRES_USER, POSTGRES_PASS,
  #      django_secret_key, admin_email, admin_password, openai_api_key_placeholder.
  #   2. Create 1Password item `khoj-oauth2-proxy` with OAUTH_CLIENT_SECRET, COOKIE_SECRET
  #      (cookie secret URL-safe base64, see project_oauth2_proxy_cookie_urlsafe.md).
  #   3. Register oauth2 client `khoj-oauth2-proxy` in Authelia config.
  # - ./khoj/ks.yaml
  # - ./khoj-oauth2-proxy/ks.yaml
