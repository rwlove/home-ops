---
apiVersion: v1
kind: Service
metadata:
  name: sync-receiver
  labels:
    app.kubernetes.io/name: sync-receiver
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sync-receiver
  ports:
    - name: ssh
      port: 2222
      targetPort: ssh
      protocol: TCP
