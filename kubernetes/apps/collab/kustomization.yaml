---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: collab
components:
  - ../../components/alerts
resources:
  - ./namespace.yaml
  - ./atuin/ks.yaml
  - ./exercisediary/ks.yaml
  # - ./nextcloud/ks.yaml
  - ./glance/ks.yaml
  - ./glance-oauth2-proxy/ks.yaml
  - ./glance-user/ks.yaml
  - ./glance-user-oauth2-proxy/ks.yaml
  - ./it-tools/ks.yaml
  - ./mealie/ks.yaml
  - ./medikeep/ks.yaml
  - ./nametag/ks.yaml
  - ./obsidian-couchdb/ks.yaml
  - ./open-webui/ks.yaml
  - ./paperless/ks.yaml
  - ./pump/ks.yaml
  # pump-cv re-enable once real RTSP cameras are wired. Currently the
  # only available pose backends ("mock" and "yolo" against a fixture
  # video file) run to completion and exit, which the Deployment
  # treats as a crashloop. Source-of-truth lives at
  # /home/rwlove/workspace/PUMP/cv/.
  # - ./pump-cv/ks.yaml
  - ./searxng/ks.yaml
  - ./sparkyfitness/ks.yaml
  - ./startpunkt-crds/ks.yaml
  # startpunkt-user + startpunkt-oauth2-proxy retired in favour of glance-user
  # at start.thesteamedcrab.com. Re-add to roll back if needed; the directories
  # remain on disk and the Authelia client / 1P entry are intact.
  - ./swiparr/ks.yaml
