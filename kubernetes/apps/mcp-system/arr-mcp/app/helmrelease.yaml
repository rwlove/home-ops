---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arr-mcp
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 1h
  values:
    defaultPodOptions:
      securityContext:
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
    controllers:
      arr-mcp:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          annotations:
            sidecar.istio.io/inject: "true"
        containers:
          app:
            image:
              repository: ghcr.io/rwlove/arr-mcp
              tag: v0.1.3@sha256:7a275ff8aab5dc4d857f80b076dc5614b3daefa0325d6110321248eb3e4d1056
            env:
              SONARR_URL: http://sonarr.media.svc.cluster.local:8989
              RADARR_URL: http://radarr.media.svc.cluster.local:7878
              LIDARR_URL: http://lidarr.media.svc.cluster.local:8686
              PROWLARR_URL: http://prowlarr.downloads.svc.cluster.local:9696
            envFrom:
              - secretRef:
                  name: arr-mcp-secret
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 256Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    service:
      app:
        controller: arr-mcp
        ports:
          http:
            port: 3000
    route:
      app:
        hostnames:
          - "arr-mcp.mcp.local"
        parentRefs:
          - name: mcp-gateway
            namespace: mcp-system
            sectionName: mcps
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /mcp
            backendRefs:
              - identifier: app
                port: 3000
