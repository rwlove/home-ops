---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: netbox-mcp
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 1h
  values:
    defaultPodOptions:
      securityContext:
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
    controllers:
      netbox-mcp:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          annotations:
            sidecar.istio.io/inject: "true"
        containers:
          app:
            # TODO: upstream netboxlabs/netbox-mcp-server has no published OCI image.
            # Build from https://github.com/netboxlabs/netbox-mcp-server (v1.1.0) and
            # push to your ZOT cache, then update this reference.
            image:
              repository: ghcr.io/netboxlabs/netbox-mcp-server
              tag: v1.1.0
            env:
              NETBOX_URL: https://netbox.${SECRET_DOMAIN}
              TRANSPORT: http
              HOST: 0.0.0.0
              PORT: "8000"
            envFrom:
              - secretRef:
                  name: netbox-mcp-secret
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 256Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    service:
      app:
        controller: netbox-mcp
        ports:
          http:
            port: 8000
    route:
      app:
        hostnames:
          - "netbox-mcp.mcp.local"
        parentRefs:
          - name: mcp-gateway
            namespace: mcp-system
            sectionName: mcps
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /mcp
            backendRefs:
              - identifier: app
                port: 8000
