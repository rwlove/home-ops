---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: beets-music-pv
  labels:
    type: nfs
spec:
  storageClassName: beets-music-storage-class
  capacity:
    storage: 3Ti
  accessModes:
    - ReadWriteMany
  nfs:
    server: "${NFS_HOST_2}"
    path: /mnt/mass_storage/storage/MP3s/

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beets-media-pvc
  labels:
    type: nfs
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 3Ti
  volumeName: beets-music-pv
  storageClassName: beets-music-storage-class
