---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app janitorr-radarr
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 30m
  install:
    disableWait: true
  upgrade:
    disableWait: true
  values:
    defaultPodOptions:
      securityContext:
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1001
        runAsNonRoot: true
        runAsUser: 1000

    controllers:
      janitorr-radarr:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/schaka/janitorr
              tag: jvm-stable@sha256:fa86a05778556958ddf070ff5ac81b50b76feb8d9db174a06d80c2b913361bf2

            envFrom:
              - secretRef:
                  name: janitorr-radarr

            resources:
              requests:
                cpu: 50m
                memory: 512Mi
              limits:
                memory: 1Gi

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    persistence:
      config:
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: ceph-block
        advancedMounts:
          janitorr-radarr:
            app:
              - path: /config

      config-file:
        type: configMap
        name: janitorr-radarr
        globalMounts:
          - path: /config/application.yml
            subPath: application.yml
            readOnly: true

      data:
        existingClaim: radarr-media
        globalMounts:
          - path: /data
            readOnly: true

      leaving-soon:
        type: emptyDir
        globalMounts:
          - path: /leaving-soon

      logs:
        type: emptyDir
        globalMounts:
          - path: /logs

      tmp:
        type: emptyDir
