---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: romm
spec:
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  refreshInterval: 15m
  target:
    name: romm-secret
    template:
      engineVersion: v2
      data:
        #ROMM_AUTH_SECRET_KEY: "{{ .ROMM_AUTH_SECRET_KEY }}"
        #ROMM_CLIENT_ID: "{{ .AUTHENTIK_CLIENT_ID }}"
        #ROMM_CLIENT_SECRET: "{{ .AUTHENTIK_CLIENT_SECRET }}"
        #IGDB_CLIENT_ID: "{{ .ROMM_IGDB_CLIENT_ID }}"
        #IGDB_CLIENT_SECRET: "{{ .ROMM_IGDB_CLIENT_SECRET }}"
        #SCREENSCRAPER_USER: "{{ .ROMM_SCREENSCRAPER_USER }}"
        #SCREENSCRAPER_PASSWORD: "{{ .ROMM_SCREENSCRAPER_PASSWORD }}"
        #RETROACHIEVEMENTS_API_KEY: "{{ .ROMM_RETROACHIEVEMENTS_API_KEY }}"
        DB_HOST: postgres-romm-rw.databases.svc.cluster.local
        DB_PORT: 5432
        DB_NAME: romm
        DB_USER: "{{ .POSTGRES_USER }}"
        DB_PASSWD: "{{ .POSTGRES_PASS }}"
        INIT_POSTGRES_DBNAME: romm
        INIT_POSTGRES_HOST: postgres-romm-rw.databases.svc.cluster.local
        INIT_POSTGRES_USER: "{{ .POSTGRES_USER }}"
        INIT_POSTGRES_PASS: "{{ .POSTGRES_PASS }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: romm
    - extract:
        key: cloudnative-pg
