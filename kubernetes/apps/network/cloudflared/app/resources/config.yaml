---
originRequest:
  originServerName: "external.${SECRET_DOMAIN}"

ingress:
  - hostname: "ssh.${SECRET_DOMAIN}"
    service: ssh://tcp.network.svc.cluster.local:22
    #originRequest:
    #  originServerName: "tcp.${SECRET_DOMAIN}"
  - hostname: "${SECRET_DOMAIN}"
    service: &svc https://external.network.svc.cluster.local:443
    #originRequest:
    #  originServerName: "external.${SECRET_DOMAIN}"
  - hostname: "*.${SECRET_DOMAIN}"
    service: *svc
  - service: http_status:404


#ingress:
#  - hostname: "*.thesteamedcrab.com"
#    originRequest:
#      http2Origin: true
#      originServerName: ingress-ext.thesteamedcrab.com
#    service: https://cilium-gateway-external.kube-system.svc.cluster.local:443
#  - service: http_status:404
