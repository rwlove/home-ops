---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: garage-data-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  storageClassName: garage-data
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NFS_HOST_0}"
    path: /mnt/kubernetes/garage/data
  mountOptions:
    - nfsvers=4.2
    - nconnect=8
    - hard
    - noatime

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garage-data-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: garage-data
  volumeName: garage-data-pv
  resources:
    requests:
      storage: 100Gi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: garage-meta-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  storageClassName: garage-meta
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NFS_HOST_0}"
    path: /mnt/kubernetes/garage/meta
  mountOptions:
    - nfsvers=4.2
    - nconnect=8
    - hard
    - noatime

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garage-meta-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: garage-meta
  volumeName: garage-meta-pv
  resources:
    requests:
      storage: 10Gi
