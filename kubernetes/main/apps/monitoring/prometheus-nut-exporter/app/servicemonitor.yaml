---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nut
  namespace: monitoring
spec:
  endpoints:
    - port: metrics
      scheme: http
      interval: 15s
      scrapeTimeout: 10s
      path: /ups_metrics
      params:
        ups:
          - ups
