# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_DOMAIN: "${DOMAIN}"
    SECRET_EMAIL: "${EMAIL}"
    SECRET_LONGHORN_BACKUP_PATH: "${LONGHORN_BACKUP_PATH}"
    SECRET_GRAFANA_PASSWORD: "${GRAFANA_PASSWORD}"
